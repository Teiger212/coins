{
  "name": "@coins/bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --target=bun --outdir=dist",
    "reap-expired": "bun run scripts/reap-expired.ts",
    "replay": "bun run scripts/replay.ts",
    "check-course-ids": "bun run scripts/check-course-ids.ts",
    "check-prices": "bun run scripts/check-prices.ts"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
